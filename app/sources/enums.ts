export enum Variant {
  PRIMARY = 'primary',
  SECONDARY = 'secondary',
  TERTIARY = 'tertiary',
}

export enum AppRoutes {
  HOME = '/',
  ABOUT = 'about',
  NOT_FOUND = '*',
}

export enum InputType {
  TEXT = 'text',
  CHECKBOX = 'checkbox',
  RADIO = 'radio',
}
