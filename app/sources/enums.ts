export enum Variant {
  PRIMARY = 'primary',
  SECONDARY = 'secondary',
  TERTIARY = 'tertiary',
  ICON = 'icon',
}

export enum AppRoutes {
  HOME = '/',
  ABOUT = 'about',
  NOT_FOUND = '*',
}

export enum InputType {
  TEXT = 'text',
  CHECKBOX = 'checkbox',
  RADIO = 'radio',
}

export enum errorCode {
  NOTFOUND = 404,
}
